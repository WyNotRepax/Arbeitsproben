import { FurnitureDirective } from './furniture.directive';

describe('FurnitureDirective', () => {
  it('should create an instance', () => {
    const directive = new FurnitureDirective();
    expect(directive).toBeTruthy();
  });
});
